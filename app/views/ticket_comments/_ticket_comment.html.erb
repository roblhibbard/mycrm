<p id="notice"><%= notice %></p>

	<div class="table-row-header">
	<h4>Enter Comments</h4>
<%= simple_form_for([@ticket_comment.ticket, @comment]) do |f| %>
  <% if @ticket_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket_comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @ticket_comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="table-row">
  	<div><%= f.hidden_field :ticket_id, :value => params[:ticket_id] %></div>
    <div class="text-input input1"><%= f.text_field :name, placeholder: 'Name'%></div><br>
    <div class="text-input"><%= f.text_area :content, placeholder: 'Comments' %></div><br>
    				<div class="text-input"><%= f.button :submit, "Submit Task", class: "btn btn-primary" %></div>
    </div>
    </div>
   <% end %>
     

 
